import { useState, useMemo } from "react";

const Home = () => {
  return (
    <div className="bg-shadowBlue w-screen">
      <samp style={{ color: "white" }}> Testing</samp>
    </div>
  );
};

export default Home;
